---
import BaseLayout from '../layouts/BaseLayout.astro';
const pageTitle = "私について";

const identity = {
  firstName: "サラ",
  country: "カナダ",
  occupation: "技術ライター",
  hobbies: [
		"写真",
		"バードウォッチング",
		"野球"
	],
};

const skills = [
		"HTML",
		"CSS",
		"JavaScript",
		"React",
		"Astro",
		"ドキュメントの執筆"
	];

const happy = true;
const finished = false;
const goal = 3;

const skillColor = "blue";
const textCase = "uppercase";
const fontWeight = "700";
---

<style define:vars={{skillColor, textCase, fontWeight}}>
	h1 {
		color: purple;
		font-size: 4rem;
	}
	.skill{
		color: var(--skillColor);
		font-weight: var(--fontWeight);
		text-transform: var(--textCase);
		font-size: 20px;
	}
</style>

<BaseLayout pageTitle={pageTitle}>
	<h2>... そして私の新しいAstroサイトについて！</h2>

	<p>以下は私に関するいくつかの事実です</p>
	<ul>
		<li>私の名前は{ identity.firstName }です。</li>
		<li>{ identity.country }に住んでおり、職業は{ identity.occupation }です。</li>
		{
			identity.hobbies.length >= 2 &&(
				<li>私の2つの趣味は{identity.hobbies[0]}と{identity.hobbies[1]}です。</li>
			)
		}
	</ul>
	<p>私のスキルは以下の通りです。</p>
	<ul>
		{
			skills &&(
				skills.map((skill)=>{
					return(
						<li class="skill">{ skill }</li>
					);
				})
			)
		}
	</ul>
	{
		happy &&
		<p>I am happy to be learning Astro! Astroを学べて幸せです！</p>
	}
	{
		finished &&
		<p>このチュートリアルを終えました</p>
	}
	{
		goal === 3 ?
		<p>My goal is to finish in 3 days. 3日で終えるのが目標です。</p>
		:
		<p>My goal is not 3 days. 3日が目標ではありません。</p>
	}
	<button id="btn">終了</button>
</BaseLayout>

<!-- 
<script>
	const btn = document.getElementById('btn');
	
	document.addEventListener('click', ()=>{
		if(finished){
			finished = false;
		}else{
			finished = true;
		}
	});
</script> -->